{% extends "base.html.twig" %}

{% block title %}Panel Admin{% endblock %}



{% block body %}

<div class="continaire-mt-4">
    <h1>Gérer les films</h1>
    <a href="{{path("filmadd")}}" class="btn btn-secondary">Ajouter un film</a>
{%for message in app.flashes('succes')%}
 <div class="alert alert-success">
    {{message}}
 </div>
{%endfor%}
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>titre</th>
                <th>Genre</th>
                <th>Actions</th>
              

            </tr>
        </thead>
        <tbody>
            {% for film in films %}
            <tr>
                <td>{{film.id}}</td>
             
                <td><a href="{{path('film',{id:film.id})}}">{{ film.titre}}</a></td>
                <td><a href="{{path("listg",{id:film.id})}}">{{film.genre.nom}}</td>
             
                <td><a href="{{path("filmed",{id:film.id})}}" class="btn btn-secondary">Modifier</a></td>
        
                <td><a href="{{path("filmsup",{id:film.id})}}" class="btn btn-secondary">Supprimer</a></td>
            </tr>
            
            
            {% endfor %}
        </tbody>
    </table>
</div>

 

{% endblock %}
